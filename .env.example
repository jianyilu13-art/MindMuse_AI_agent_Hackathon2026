# Copy this file to `.env` and fill in your key to enable live Google Shopping.
# `.env` is git-ignored — never commit your real key.
#
#   cp .env.example .env
#
# Get a key at https://www.searchapi.io (free trial credits).
SEARCHAPI_API_KEY=
